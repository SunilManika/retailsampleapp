apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retail-postgres-pvc
  namespace: NAMESPACE_PLACEHOLDER
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
