apiVersion: apps/v1
kind: Deployment
metadata:
  name: retail-frontend
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app: retail-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: retail-frontend
  template:
    metadata:
      labels:
        app: retail-frontend
    spec:
      serviceAccountName: NAMESPACE_PLACEHOLDER
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: frontend
          image: docker.io/technologybuildingblocks/retail-frontend:1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
