apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rag-retrieval-api
  namespace: NAMESPACE_PLACEHOLDER
spec:
  to:
    kind: Service
    name: rag-retrieval-api
  port:
    targetPort: http
  tls:
    termination: edge


